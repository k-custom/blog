```js
    function Com() {
        const [schoolValue, setSchoolValue] = useState('')
        const debounced = fn => {
            let timer = null
            return function() {}
        }
    }
```